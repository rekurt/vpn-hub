# `ip netns add` bind-mounts namespace handles into /run/netns. The agent runs with
# ProtectSystem=strict, so the directory must exist and be listed in ReadWritePaths
# before the unit starts. Install to /usr/lib/tmpfiles.d/vpn-hub.conf.
d /run/netns 0755 root root -
